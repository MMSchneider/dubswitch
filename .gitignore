# ── Node & Package Managers ───────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── Logs ──────────────────────────────────────────────────────────────────
logs
*.log

# ── Diagnostic reports (Node) ─────────────────────────────────────────────
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# ── Runtime data ──────────────────────────────────────────────────────────
pids
*.pid
*.seed
*.pid.lock

# ── Coverage / Instrumentation ────────────────────────────────────────────
coverage/
*.lcov
.nyc_output
lib-cov

# ── Build / Output ────────────────────────────────────────────────────────
dist/
build/
out/
*.tsbuildinfo

# ── Tool & Framework caches ───────────────────────────────────────────────
.cache/
.parcel-cache/
.eslintcache
.stylelintcache
.turbo/

# Next.js / Nuxt / Gatsby / Vitepress / VuePress / SvelteKit
.next/
out/
.nuxt/
.cache/
.vuepress/dist
.temp
**/.vitepress/dist
**/.vitepress/cache
.docusaurus
.svelte-kit/

# Vercel / Netlify
.vercel/
.netlify/

# ── Package managers: Yarn v3 (PnP) ───────────────────────────────────────
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# ── Dependency directories (legacy tools) ─────────────────────────────────
jspm_packages/
bower_components/
web_modules/           # Snowpack

# ── Serverless & Cloud Emulators ──────────────────────────────────────────
.serverless/
.dynamodb/
.firebase/

# ── Bundlers / Misc tools ─────────────────────────────────────────────────
.fusebox/
.tern-port
.vscode-test

# ── Env files (keep a template) ───────────────────────────────────────────
.env
.env.*
!.env.example

# ── Editors / IDEs / OS cruft ─────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.iml

# VS Code — ignore everything except shared settings/tasks/launch/extensions
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

# ── Packaging / Artifacts (Electron / installers / zips) ──────────────────
# Electron / builder outputs (uncomment if you package desktop apps)
# dist_electron/
*.dmg
*.exe
*.AppImage
*.snap
*.deb
*.rpm
*.msi
*.blockmap
latest*.yml

# Output of `npm pack`
*.tgz

# ── Vite timestamp logs ───────────────────────────────────────────────────
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# ── Release archives (DubSwitch) ──────────────────────────────────────────
# General catch-all for your CI/CD packaging
dist-release-*
dubswitch-*-*.zip
